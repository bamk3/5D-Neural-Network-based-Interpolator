# =============================================================================
# Environment Configuration for 5D Interpolator
# =============================================================================
# Copy this file to .env and customize the values for your environment
# cp .env.example .env

# =============================================================================
# BUILD CONFIGURATION
# =============================================================================

# Build target: development, production, or testing
# - development: Includes dev dependencies, hot reload, verbose logging
# - production: Optimized build, no dev dependencies, minimal logging
# - testing: Includes test dependencies, runs tests on build
BUILD_TARGET=development

# Volume mount mode: rw (read-write) or ro (read-only)
# Use 'rw' for development to enable hot reload
# Use 'ro' for production for better security
VOLUME_MODE=rw

# =============================================================================
# APPLICATION ENVIRONMENT
# =============================================================================

# Application environment: development, staging, or production
APP_ENV=development

# Node environment: development or production
NODE_ENV=development

# Enable debug mode (true/false)
DEBUG=true

# Logging level: debug, info, warning, error, critical
LOG_LEVEL=info

# =============================================================================
# NETWORK CONFIGURATION
# =============================================================================

# Backend port (exposed to host)
BACKEND_PORT=8000

# Frontend port (exposed to host)
FRONTEND_PORT=3000

# API URL for frontend to connect to backend
# In development (from host): http://localhost:8000
# In development (from container): http://backend:8000
# In production: https://your-domain.com/api
NEXT_PUBLIC_API_URL=http://localhost:8000

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# Allowed CORS origins (comma-separated)
# For development: http://localhost:3000
# For production: https://your-domain.com
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# =============================================================================
# BACKEND CONFIGURATION
# =============================================================================

# Number of uvicorn workers (for production)
# Recommended: 2-4 workers per CPU core
WORKERS=4

# Maximum upload file size in bytes (default: 10MB)
MAX_UPLOAD_SIZE=10485760

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================

# Disable Next.js telemetry (true/false)
NEXT_TELEMETRY_DISABLED=1

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================

# Docker Compose project name
COMPOSE_PROJECT_NAME=interpolator

# Docker registry (for pushing images)
# DOCKER_REGISTRY=your-registry.com
# DOCKER_USERNAME=your-username

# =============================================================================
# PRODUCTION SETTINGS (uncomment for production)
# =============================================================================

# BUILD_TARGET=production
# APP_ENV=production
# NODE_ENV=production
# DEBUG=false
# LOG_LEVEL=warning
# VOLUME_MODE=ro
# NEXT_PUBLIC_API_URL=https://api.your-domain.com
# CORS_ORIGINS=https://your-domain.com
